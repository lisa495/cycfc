spring:
  boot:
    admin:
      client:
        url: http://localhost:8800

management:
  metrics:
    web:
      server:
        auto-time-requests: false
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always